const inquirer = require('inquirer');
const { join } = require('path');
const {writeFile} = require('fs');
const { createDocument } = require('./createDocument');


function createSvgLogo (width, height, color, shape){

}

module.exports = { createSvgLogo };